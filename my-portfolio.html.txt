<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>F Lalchawiliana - Professional Slacker & Cookie Enthusiast</title>
    <style>
        :root {
            --primary-color: #ff69b4;
            --secondary-color: #00ff00;
        }

        body {
            font-family: 'Comic Sans MS', cursive;
            background: linear-gradient(45deg, #ff9999, #99ff99);
            margin: 0;
            padding: 20px;
        }

        .header {
            text-align: center;
            padding: 50px;
            background: url('data:image/svg+xml,<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg"><rect width="100" height="100" fill="%23ff0000" opacity="0.1"/></svg>');
        }

        .profile-img {
            width: 200px;
            border-radius: 50%;
            border: 5px dashed var(--primary-color);
            transition: transform 0.3s;
        }

        .profile-img:hover {
            transform: rotate(360deg) scale(1.1);
        }

        .skill-bubble {
            display: inline-block;
            padding: 10px 20px;
            margin: 5px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50px;
            border: 2px solid var(--primary-color);
            transition: transform 0.2s;
        }

        .project-card {
            background: rgba(255, 255, 255, 0.8);
            padding: 20px;
            margin: 20px;
            border-radius: 10px;
            transition: all 0.3s;
        }

        .project-card:hover,
        .skill-bubble:hover {
            transform: rotate(3deg) scale(1.05);
            box-shadow: 5px 5px 15px rgba(0,0,0,0.3);
        }

        #coffee-counter {
            position: fixed;
            bottom: 10px;
            right: 10px;
            background: white;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
            font-size: 14px;
        }

        #meme-button {
            position: fixed;
            bottom: 20px;
            left: 20px;
            padding: 15px;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            font-size: 20px;
        }
    </style>
</head>
<body>
    <header class="header">
        <h1>👋 Hi! I'm F Lalchawiliana</h1>
        <img src="your-photo.jpg" alt="Portrait of F Lalchawiliana" class="profile-img">
        <h2>Professional Overthinker • Part-time Jedi • Full-time Snack Connoisseur</h2>
    </header>

    <section>
        <h2>🦸 My Superpowers</h2>
        <div class="skill-bubble">Certified Googler</div>
        <div class="skill-bubble">Ctrl+C / Ctrl+V Expert</div>
        <div class="skill-bubble">Professional Napper</div>
        <div class="skill-bubble">Microwave Chef</div>
        <div class="skill-bubble">Error Message Translator</div>
    </section>

    <section>
        <h2>💻 Notable Achievements</h2>
        <div class="project-card">
            <h3>World Record: 37 Browser Tabs Open Simultaneously</h3>
            <p>Chrome cried. My laptop wept. I prevailed.</p>
        </div>
        <div class="project-card">
            <h3>Invented New Programming Language: JavaSleep</h3>
            <p>It's like Java, but with more naps</p>
        </div>
    </section>

    <button id="meme-button" aria-label="Click for a meme">🤣</button>
    <div id="coffee-counter">☕ Coffees consumed today: <span id="coffee-count">0</span><br>(Press spacebar to add one!)</div>

    <script>
        let coffeeCount = 0;

        const memeActions = [
            () => alert("Why did the programmer quit their job? Because they didn't get arrays!"),
            () => alert("I would tell you a UDP joke, but you might not get it."),
            () => document.body.style.transform = 'rotate(180deg)',
            () => document.body.style.filter = 'hue-rotate(180deg)'
        ];

        document.getElementById('meme-button').addEventListener('click', () => {
            const randomIndex = Math.floor(Math.random() * memeActions.length);
            memeActions[randomIndex]();
        });

        document.addEventListener('keypress', (e) => {
            if (e.code === 'Space') {
                coffeeCount++;
                document.getElementById('coffee-count').textContent = coffeeCount;
            }
        });
    </script>
</body>
</html>
